name: Pack CT snapshot from prod

# Weekly pack of latest prod libraries → snapshot bucket.
# Needs: vars.CT_SNAPSHOT_BUCKET, secrets.TOOLS_REPO_TOKEN, IAM on Github-Actions.

on:
  schedule:
    - cron: '0 6 * * 1' # Mondays 06:00 UTC
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

jobs:
  pack-and-upload:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          repository: h5p/h5pcom-tools
          token: ${{ secrets.TOOLS_REPO_TOKEN }}

      - uses: actions/setup-node@v6
        with:
          node-version: 24

      - run: npm ci

      - uses: aws-actions/configure-aws-credentials@v6
        with:
          role-to-assume: arn:aws:iam::694349725181:role/Github-Actions
          aws-region: us-west-1

      - name: Pack and upload
        env:
          SNAPSHOT_BUCKET: ${{ vars.CT_SNAPSHOT_BUCKET }}
        run: |
          DATE=$(date -u +%Y-%m-%d)
          OUT="h5ps-from-prod-${DATE}.h5p"
          DEST="s3://${SNAPSHOT_BUCKET}/ct-snapshots/prod"

          node h5pcom-pack-latest-libraries-ci.mjs "$OUT"

          aws s3 cp "$OUT" "$DEST/$OUT"
          aws s3 cp "$OUT.manifest.json" "$DEST/$OUT.manifest.json"
          aws s3 cp "$OUT" "$DEST/latest.h5p"
          aws s3 cp "$OUT.manifest.json" "$DEST/latest.h5p.manifest.json"

      - uses: actions/upload-artifact@v4
        with:
          name: ct-snapshot
          path: h5ps-from-prod-*.h5p*
          retention-days: 14
